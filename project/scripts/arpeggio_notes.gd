extends Node

@export_enum("noteC", "noteD", "noteE", "noteF", "noteG") var Notes = 0



# what does this need to do?
# get what notes the player plays, match to note defined in enum, determine outcome

# enum 
# can't put invalid input 
# enum that corresponds to action want, have it connect to string of pressed input
# can make equal to objects

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
